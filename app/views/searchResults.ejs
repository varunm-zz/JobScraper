<!DOCTYPE html>
<html>
	<head>
		<title>Search Results</title>
	</head>
	<body>
		<h1>Search Results for <%= title %> </h1>
		<% if(results) { %>
			<%= JSON.stringify(results) %>
		<% } %>
	</body>
</html>
